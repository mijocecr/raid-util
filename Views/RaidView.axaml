<UserControl
    x:Class="RAID_Util.Views.RaidView"
    x:Name="RaidViewControl"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <!-- ⭐ GRID PRINCIPAL -->
    <Grid RowDefinitions="Auto,*"
          Margin="10"
          RowSpacing="10">

        <!-- ⭐ HEADER FIJO (NO SCROLL) -->
        <Border Grid.Row="0"
                Background="{DynamicResource BMWSurfaceElevatedBrush}"
                Padding="12"
                CornerRadius="4"
                BorderBrush="{DynamicResource BMWBorderBrush}"
                BorderThickness="1">

            <Grid ColumnDefinitions="*,*,*"
                  RowDefinitions="Auto,Auto"
                  ColumnSpacing="10"
                  RowSpacing="10">

                <!-- FILA 1 -->
                <Button Grid.Row="0" Grid.Column="0"
                        Content="Create Array"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Center"
                        BorderBrush="{DynamicResource BMWAccentBrush}"
                        x:Name="BtnCreateArray"
                        Classes="HeaderButton" />

                <Button Grid.Row="0" Grid.Column="1"
                        Content="⟳ Refresh"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Center"
                        BorderBrush="{DynamicResource BMWAccentBrush}"
                        x:Name="BtnRefreshArrays"
                        Classes="HeaderButton" />

                <Button Grid.Row="0" Grid.Column="2"
                        Content="Config"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Center"
                        x:Name="BtnConfigArrays"
                        BorderBrush="{DynamicResource BMWAccentBrush}"
                        Classes="HeaderButton" />

                <!-- FILA 2 -->
                <Button Grid.Row="1" Grid.Column="0"
                        Content="Assemble"
                        HorizontalAlignment="Stretch"
                        x:Name="BtnAssembleArrays"
                        HorizontalContentAlignment="Center"

                        BorderBrush="{DynamicResource BMWHealthHealthyBrush}"

                        Classes="HeaderButton" />

                <Button Grid.Row="1" Grid.Column="1"
                        Content="Initialize"
                        HorizontalAlignment="Stretch"
                        x:Name="BtnInitialize"
                        HorizontalContentAlignment="Center"

                        BorderBrush="{DynamicResource BMWHealthWarningBrush}"
                        Classes="HeaderButton" />

                <Button Grid.Row="1" Grid.Column="2"
                        Content="Delete Array"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Center"
                        x:Name="BtnDeleteArray"
                        BorderBrush="{DynamicResource BMWHealthCriticalBrush}" />


            </Grid>
        </Border>

        <!-- ⭐ SOLO LA LISTA SCROLLEA -->
        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="ListArrays"
                        Spacing="8" />
        </ScrollViewer>

    </Grid>

</UserControl>